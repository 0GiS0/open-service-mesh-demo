apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: moviestore-ingress
  namespace: stores
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: moviestore.<PUBLIC_IP_INGRESS>.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: moviestore
                port:
                  number: 80

---

kind: IngressBackend
apiVersion: policy.openservicemesh.io/v1alpha1
metadata:
  name: moviestore-external-access
  namespace: stores
spec:
  backends:
    - name: moviestore
      port:
        number: 3000 # targetPort of the service
        protocol: http
  sources:
    - kind: Service
      name: osm-nginx-ingess-ingress-nginx-controller
      namespace: ingress-nginx
